name: earthflu
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  cloud_firestore: ^6.3.0
  firebase_core: ^4.7.0
  flutter:
    sdk: flutter
  flutter_map: ^6.0.0 # Harita motoru
  latlong2: 0.9.1    # Koordinat hesaplamaları için
  flutter_map_tile_caching: ^9.0.0 # ÇEVRİMDIŞI ÇALIŞMA İÇİN KRİTİK
  geolocator: ^10.1.0 # Konum almak için
  workmanager: ^0.9.0

  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
